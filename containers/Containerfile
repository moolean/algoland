FROM python:3.11-slim
RUN apt-get update && apt-get install -y --no-install-recommends \
    git openssh-client curl jq make vim ca-certificates \
    && rm -rf /var/lib/apt/lists/*
WORKDIR /workspace
ENV PIP_DISABLE_PIP_VERSION_CHECK=1
